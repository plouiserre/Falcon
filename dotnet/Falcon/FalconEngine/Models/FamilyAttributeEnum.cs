namespace FalconEngine.Models
{
    public enum FamilyAttributeEnum
    {
        accesskey, asAttr, blocking, charset, classCss, content, contenteditable, crossorigin, data_, disabled,
        download, draggable, dir, httpequiv, href, hreflang, id, integrity, lang, manifest, media, name, rel,
        referrerpolicy, role, scheme, spellcheck, sizes, style, tabindex, target, title, translate, type, xmlns
    }
}