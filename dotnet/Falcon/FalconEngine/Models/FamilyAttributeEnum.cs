namespace FalconEngine.Models
{
    public enum FamilyAttributeEnum
    {
        accesskey, asAttr, blocking, charset, classCss, content, contenteditable, crossorigin, datatheme, datapage,
        datapreload, datapurpose, datauser, draggable, dir, disable, httpequiv, href, hreflang, id, integrity, lang,
        manifest, media, name, rel, referrerpolicy, role, scheme, spellcheck, sizes, style, tabindex,
        title, translate, type, xmlns
    }
}